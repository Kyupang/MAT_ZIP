<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boss">

	<!-- insert, delete, update는 무조건 resultType = "int"이므로 프로그래머가 써주지 않아도 이미 
		resultType = "int"로 본다.! -->

	<insert id="innerJoinAndInsert" parameterType="boss_memberVO">
	insert into boss_member
	select m.user_id, m.password, #{store_id}, m.regdate, m.nickname
	from mz_member m
	where m.user_id = #{user_id}
</insert>

<!-- 	<delete id="del" parameterType="String">
		delete from boss_member where id
		= #{id}
	</delete>

	<update id="up" parameterType="boss_memberVO">
		update boss_member set tel =
		#{tel} where id = #{id}
	</update>

	<select id="one" parameterType="String"
		resultType="boss_memberVO">
		select * from boss_member where id = #{id}
	</select>
	<select id="list" resultType="boss_memberVO">
		select * from boss_member
	</select> -->
	<select id="boss_login" parameterType="String" resultType="boss_memberVO">
    select * from boss_member where user_id = #{user_id} and password = #{password}
</select>
	<!-- vo와 db table의 같을 때는 자동으로 컬럼명과 동일한 vo의 변수에 자동으로 넣어줌 . vo와 db table이 
		다를 때는 수동으로 어떤 컬럼명에서 꺼내서 vo의 변수에 넣어줄지 mapping을 해주어야 함. -->



</mapper>











